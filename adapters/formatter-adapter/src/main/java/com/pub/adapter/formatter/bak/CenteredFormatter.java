package com.pub.adapter.formatter.bak;

import com.pub.domain.port.LineFormatter;

public class CenteredFormatter implements LineFormatter {
    @Override
    public String format(String input) {
        return input;
    }
}
