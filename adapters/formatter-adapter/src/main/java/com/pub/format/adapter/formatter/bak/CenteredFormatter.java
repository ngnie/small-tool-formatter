package com.pub.format.adapter.formatter.bak;

import com.pub.format.modules.domain.port.LineFormatter;

public class CenteredFormatter implements LineFormatter {
    @Override
    public String format(String input) {
        return input;
    }
}
